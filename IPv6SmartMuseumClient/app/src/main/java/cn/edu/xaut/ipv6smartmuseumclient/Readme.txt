一些注意事项

1.OkHttp的Call中，call.enqueue()方法应该会直接将请求开始执行，那样的话就不需要手动执行了，这点需要测试。

2.Picasso加载图片时，缩放需要手动计算，否则会因为图片过大导致闪退。Placeholder需要美化一下（它的大小不会自动缩放）

3.目前自定义Adapter所需的数据（比如各种列表）是通过setter加进去的，可以写新的构造方法来减少多余步骤，也可以防止少些某些set方法。